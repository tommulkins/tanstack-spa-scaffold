pre-commit:
  # Tiered gates — ADR 0001: fast checks only; full pnpm test before merge (not here)
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    agent-policy:
      run: bash scripts/check-agent-suppressions.sh --staged
